SB_CONFIG_BOOTLOADER_MCUBOOT=y
SB_CONFIG_BOOT_SIGNATURE_TYPE_ECDSA_P256=y
# See sysbuild.conf's comment: the external-flash secondary slot is declared
# via devicetree now, not the deprecated Partition Manager sysbuild options.

SB_CONFIG_BOOT_SIGNATURE_KEY_FILE="${APP_DIR}/keys/private/boot-ecdsa-p256.pem"
